const userInputErrors = {
  INVALID_INPUT: 'Invalid input.',
}

const countryErrors = {
  // User forms
  COUNTRY_ALREADY_EXISTS: 'The country you are trying to add already exists.',
}

export {
  userInputErrors,
  countryErrors,
}
