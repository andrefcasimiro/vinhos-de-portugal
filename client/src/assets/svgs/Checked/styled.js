// @flow
import styled from "styled-components"
import theme from "global/theme"

export const StyledSVG = styled.svg`
  fill: ${theme.color.veryDarkerGray};
  border: 1px solid ${theme.color.creme};
`
